# LEARNINGS — task-add-table-column

> Episodic memory. Append `- [YYYY-MM-DD] (source: user|inference) <learning>`.
> Distilled into SKILL.md by the consolidation step once a pattern is stable.

- [2026-06-25] (source: inference) Seed entry — no task runs yet. The `costBenefit`
  (`I/$`) column is the reference example of a fully computed column (getValue/format/color
  all recompute the same ratio); copy its structure for derived metrics.
