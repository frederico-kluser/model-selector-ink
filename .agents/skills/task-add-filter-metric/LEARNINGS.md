# LEARNINGS — task-add-filter-metric

> Episodic memory. Append `- [YYYY-MM-DD] (source: user|inference) <learning>`.
> Distilled into SKILL.md by the consolidation step once a pattern is stable.

- [2026-06-25] (source: inference) Seed entry — no task runs yet. `i/$` and
  `costbenefit` both map to `computeCostBenefit`; multiple aliases → one accessor is
  the established pattern. Keep accessor and the matching column's `getValue` logic in sync.
