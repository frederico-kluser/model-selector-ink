# LEARNINGS — task-release-package

> Episodic memory. Append `- [YYYY-MM-DD] (source: user|inference) <learning>`.
> Distilled into SKILL.md by the consolidation step once a pattern is stable.

- [2026-06-25] (source: inference) Seed entry — no release run yet. The script
  intentionally separates publish from `git push`; a green publish still needs a
  manual `git push && git push --tags`.
